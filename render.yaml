services:
  - type: web
    name: stellantis-training-processor
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python wsgi.py
    plan: free
    autoDeploy: true
    branch: main
